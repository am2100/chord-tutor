/* jslint settings */

// Module /ct/
//
var ct = (function ( $ ) {

  // Module scope variables
  var
    // Set constants
    configMap = { }
    // Declare all other module scope variables
    
  // DOM methods

  // Event handlers

  // Public method /initModule/
  initModule = function ( $container ) {

    // Render HTML
    // Initialize
    // Bind events to event handlers
  };
}());

// Start ct once DOM is loaded
